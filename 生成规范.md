# 风格规范

> 适用范围：本项目所有 HTML PPT 页面（当前为 `app/core/index.html`）。
> 目标：任何新增/修改页面，都必须遵循以下红色主题规范，保证全套幻灯片风格统一。

## 1. 品牌色板（只允许使用以下颜色）

| 角色 | 色值 | 用途 |
|---|---|---|
| 主红 | `#D6000F` | 页面标题、核心强调、页眉条渐变亮端、徽章、表头 |
| 深红 | `#8F000A` | 页眉条/横幅渐变暗端、次级阶段色 |
| 辅蓝 | `#1B6FD6` | 次级强调、并列第二元素、链接性文字 |
| 辅蓝浅 | `#4FA3FF` | 深色底上的蓝色强调文字 |
| 正文黑 | `#2B2B2B` | 正文文字 |
| 辅助灰 | `#8A8A8A` | 副标题、说明性文字 |
| 浅红底 | `#FBF3F3` | 红色系卡片底色 |
| 浅蓝灰底 | `#F5F7FA` | 蓝色系卡片底色、公式条底色 |
| 深卡底 | `#3B3B3B → #1E1E1E` 渐变 | 深色工程卡（配浅色文字 `#B9B9B9`） |

- 禁止引入色板之外的颜色（如原视频稿的绿/紫系一律不用）。
- 金色仅允许 `#FFD166` 一处：红色横幅内的关键词高亮。

## 2. 红蓝分工（重要约定）

- **红 = 主线/结论/第一元素**，**蓝 = 辅线/过程/第二元素**。
- 并列结构超过 2 个时，按 红→蓝→红（或红→深红→红）交替，体现"视觉重心逐步收敛到红"（参考第1页三阶段：蓝→深红→主红）。
- 深色卡片上的强调文字：红系用 `#FF8A90`，蓝系用 `#4FA3FF`。

## 3. 页面结构（内容页统一骨架）

1. **页眉条**：高 44px，`主红→深红` 横向渐变；左侧白色菱形 + 章节名，右侧页码 `N / 总数`。
2. **标题区**：主标题 34px 主红加粗；副标题 17px 辅助灰，其中关键词用辅蓝加粗（`<em>`）。
3. **正文区**：左右边距 56px，垂直居中，底部留白 56px。
4. 舞台固定 1280×720（16:9），整体按窗口缩放。

## 4. 组件规范

- **层级徽章**（如 L1/表达层）：主红或辅蓝实底白字，圆角 3-4px，字号 12-14px，加粗。
- **要点列表**：红方块（主线）/ 蓝圆点（辅线）作符号，行距 ≥1.55，字号 15-16.5px；要点中的关键词：红色 `<b>` 或蓝色加粗。
- **卡片**：浅底 + 5-6px 左侧色边 + 圆角 6px；红卡标题深色、蓝卡标题辅蓝。
- **结论横幅**：`主红→深红` 渐变底白字 18px，圆角 6px，关键词 `#FFD166` 加粗；用于每页的一句话结论。
- **深色工程卡**：深灰渐变底，标题白字 + 红/蓝徽章，正文 `#B9B9B9`。
- **流程步骤**：白底 2px 主红描边圆角框 + 红色箭头串联；循环用辅蓝标注回环。
- **表格**：表头 `主红→深红` 渐变白字，行分隔 `#eee` 细线，关键词主红加粗。
- **包含关系图**：同心圆由外到内 = 主红（Harness）→ 辅蓝（Context）→ 黑（Prompt）。

## 5. 字体与文字

- 字体栈固定：`"Microsoft YaHei","PingFang SC","Noto Sans CJK SC",sans-serif`。
- 字号阶梯：主标题 34 / 卡片标题 16.5-19 / 正文 15-16.5 / 辅助 13-14 / 徽章 11.5-14。
- 强调手法只有三种：主红加粗、辅蓝加粗、横幅内金色加粗；不使用斜体、下划线、多彩混排。

## 6. 交互规范

- 键盘 ←/→（含 ↑/↓、PageUp/Down、空格）翻页，Home/End 跳首尾。
- URL 参数 `?p=N`（1 起）直达指定页，翻页时 `history.replaceState` 同步。
- 右下角圆形上一页/下一页按钮 + 页码指示；底部 3px 红→蓝进度条。
- 无音频、无自动播放，纯手动演讲翻页。

## 7. 封面/章节页（预留，待 Part 三/四时使用）

- 红色渐变底（主红→深红，斜向）+ 白色标题 + 辅蓝细线点缀 + 右上角"T 字上扬"折线装饰（抽象化，不使用官方 logo 图形）。

# 操作规范
- 如果有删除或者替换的页面，先统一挪到最后面去，方便后续查找，不要直接删。
