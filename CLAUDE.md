# 页面版式风格库（生成指导）

> 背景：连续多页使用同一种版式（如 P15/P16/P17 的「卡片 + pills + 横幅」堆叠）会造成视觉疲劳。
> 生成新页面时，先从下面 5 种版式中挑选最贴合内容结构的一种，并遵守：
> - **相邻 3 页不允许同版式**；同一章节内至少使用 2 种版式交替。
> - 所有版式都只使用 `生成规范.md` 的色板与组件，变化的是**布局节奏**，不是配色。
> - 红色结论横幅（banner）不是每页必备：一章内 1-2 处即可，留给真正的结论页。

**规范**：
- 对于大幅度的修改 自行截图确认没问题再交付。
- 主、副标题，内容文字需要言之有物，不要泛泛之谈、用夸张虚构的词，要逻辑严谨、凝练和简介。
- 不要用checkout回滚， 根据历史内容回滚。

## 寻页规则（用户说"PN / 第N页"时严格按此执行）

**所有"PN"中的 N 一律指 DOM 顺序**（浏览器右下角 `X / Y`、URL `?p=N`、JS 里 `querySelectorAll('.slide')` 的索引），**与文件里 `<section id="slide-pX">` 的 X 无关**——那是历史遗留命名，已和真实位置偏移约 2 页。


## 单文件打包（分享 / 离线演示）[手动触发]

`app/core/index.html` 同目录引用的 PNG/JPG/WebP/GIF（目前 7 张图）会被脚本内联为 `data:` URI，产出**一个双击就能在浏览器打开的 `.html`**。原 `index.html` 不动，方便继续编辑。

```bash
python3 app/core/build-bundled.py
# 输出：app/core/index.bundled.html  （含 5 张图，约 2.0 MB）
```

脚本逻辑（`app/core/build-bundled.py`，如要扩展内联其它资源在此维护）：

1. 扫描 `<img src="...">` 中以 png/jpg/jpeg/webp/gif 结尾的相对路径；
2. 同目录下找不到对应文件则跳过（不报错，留原 src）并在 stderr 列出；
3. 替换为 `data:image/<ext>;base64,<...>` 后写回 `index.bundled.html`。

**验证残留**：打包后用

```bash
grep -oE 'src="[^"]+"' app/core/index.bundled.html | grep -v '^src="data:' | grep -v '^src="http'
```

应输出为空（只剩 http 外链如 rtk GitHub）。

**适用场景**：邮件 / 微信 / 钉钉 / U盘分享、归档交付；不要二次编辑——继续改还是回到 `index.html`，改完重跑脚本即可。

---

## 风格 A · 卡片矩阵式（Grid）

- **结构**：标题区 → 2×2 或 1×3 卡片网格 → （可选）横幅。
- **适用**：并列对比 2-4 个同类对象（分类、方法对比、优缺点）。
- **要点**：卡片必须严格同类、同构；超过 4 个并列项就改用风格 E 表格。
- **现有示例**：P14 按 Agent 类型选评测方法、P15 打分器三层。
- **注意**：这是当前 deck 里用得最多的版式，新增页面优先考虑其他风格。

## 风格 B · 左右分栏式（Split）

- **结构**：标题区 → 左图右文（或左文右图），图侧占 45-55%。
- **适用**：「数据/图示 + 解读」「案例 + 定义」「公式 + 说明」这类一主一辅的内容。
- **要点**：图侧可以是 SVG 图、深色工程卡、代码块；文侧用 2-3 张纵向堆叠的小卡或要点列表，不要再切成网格。
- **现有示例**：P4（案例 + 公式定义）、P16 上半（pass@k 曲线 + 公式卡）。

## 风格 C · 流程链路式（Flow）

- **结构**：标题区 → 横向步骤链（白底红描边步骤框 + 箭头，可带回环/分支），下方一行备注或反例。
- **适用**：流程、闭环、生命周期、演进路线、「A 毕业转入 B」这类有方向性的内容。
- **要点**：步骤 3-5 个为宜；回环用辅蓝标注；需要阶段对比时做成上下两条链（如能力线/回归线）。
- **现有示例**：P13 四步评估闭环、P17 中段「30%→80%→95%→毕业」。

## 风格 D · 中心图解式（Diagram）

- **结构**：标题区 → 一张占正文 60-70% 的大图（架构图、映射图、同心圆、双层全景），图内/图缘直接标注文字。
- **适用**：架构组成、映射关系、系统全景——信息本身有空间结构时，图比卡片更直接。
- **要点**：图是主角，不再配卡片矩阵；标注文字控制在图内和图缘，必要时可配 FLIP 重排动画（见 P5）。
- **现有示例**：P1 演进全景双层图、P3 同心圆、P5 冯诺依曼映射 ⇄ Agent 全景。

## 风格 E · 归纳表格式（Table）

- **结构**：标题区 → 一张全宽表格（红渐变表头，`#eee` 行线）→ 一行结论。
- **适用**：多对象 × 多维度归纳、决策清单、章节小结。
- **要点**：列数 ≤ 4，单元格文字 ≤ 2 行；关键词主红加粗，次要信息灰色。
- **现有示例**：P12 六层小结映射表。

## 风格 F · 大字观点式（Statement）

- **结构**：标题区 → 居中大字（定义/金句/大数字/公式）+ 一条 quote 引用条 + 极简注脚。
- **适用**：章节引入页、核心定义页、转折页——需要观众停下来记住一句话的时候。
- **要点**：正文元素 ≤ 3 个；字号可以破格放大（主结论 28-40px）；不放卡片网格。
- **现有示例**：P4 的「如果你不是模型，那你就是 Harness」、P13 的引入 quote。

## 节奏建议

- 一个 4-5 页的章节，推荐组合：引入页（F 或 C）→ 分类页（A 或 E）→ 机制页（B 或 D）→ 收尾页（C 或 F）。
- 连续两页信息密度一高一低交替：图解页/大字页（低）之后接表格/卡片页（高），再回来。
